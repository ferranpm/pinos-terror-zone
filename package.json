{
  "name": "pinos-terror-zone",
  "version": "1.0.0",
  "description": "Binding of Isaac like simple game",
  "repository": "git@github.com:ferranpm/pinos-terror-zone",
  "author": "Ferran Pelayo Monfort <ferran.pel.mon@gmail.com>",
  "license": "MIT",
  "scripts": {
    "dev": "esbuild src/main.js --bundle --sourcemap --platform=browser --watch --outfile=dist/game.min.js",
    "build": "esbuild src/main.js --bundle --sourcemap --platform=browser --minify --outfile=dist/game.min.js"
  },
  "devDependencies": {
    "esbuild": "^0.17.2"
  }
}
